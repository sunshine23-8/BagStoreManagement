# ===========================================
# Database Configuration - SQL Server
# ===========================================
db.url=jdbc:sqlserver://localhost:1433;databaseName=BagStoreDB;encrypt=true;trustServerCertificate=true;integratedSecurity=true
db.username=
db.password=

# ===========================================
# Application Settings
# ===========================================
# Pending order timeout in minutes (configurable by admin)
app.pending.timeout.minutes=5

# Low stock warning threshold
app.inventory.low.stock.threshold=3
